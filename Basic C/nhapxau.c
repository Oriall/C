#include <stdio.h>
#include <string.h>
int main() {
    int dem=1;
    char s[100];
    while (fgets(s, sizeof(s), stdin) != NULL) {
        dem++;
               
        }
    
    printf("%d",dem);
}
